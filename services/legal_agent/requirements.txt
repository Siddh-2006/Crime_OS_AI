# ── Core (always required) ────────────────────────────────────────────────────
fastapi==0.115.6
fitz==0.0.0
httpx==0.28.1
numpy==2.4.6
pydantic==2.10.6
qdrant_client==1.9.2
uvicorn[standard]==0.30.0

# ── Development only — local embedding models ─────────────────────────────────
# Not installed in production (EMBEDDING_BACKEND=gemini uses httpx above instead).

# BGE / sentence-transformers (primary local embedder)
# sentence_transformers==5.7.0

# PyTorch (required by sentence-transformers and transformers)
# torch==2.5.1
# transformers==4.41.2

# PaddleOCR — only needed for one-time legal corpus ingestion scripts
# paddleocr==3.7.0
# paddlepaddle>=2.6.1
